# Local PostgreSQL for development — read by docker-compose.yml.
# Keep DATABASE_URL in apps/api/.env in sync with these values.
POSTGRES_USER=pitstop
POSTGRES_PASSWORD=pitstop
POSTGRES_DB=pitstop
POSTGRES_PORT=5433
