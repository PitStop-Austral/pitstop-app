## Summary

<!-- Describe what changed and why. -->

-

## Verification

- [ ] I tested the affected behavior locally.
- [ ] I added or updated tests when applicable.

## Definition of Done

- [ ] The local database starts with `docker compose up -d`.
- [ ] `pnpm install` completes and installs the Git hooks automatically.
- [ ] `pnpm dev` starts both apps on the documented ports.
- [ ] `pnpm format:check`, `pnpm lint`, `pnpm check-types`, `pnpm test`, and `pnpm build` pass from the repository root.
